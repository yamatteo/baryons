opts = dict(
    b1=0.9,
    b2=0.999,
    batch_size=64,
    # channels=1,
    checkpoint_interval=-1,
    # criterion_gan="mse",
    # criterion_pixelwise="l1",
    data_path=r"data",
    dataset_path=r"dataset",
    decay_epoch=400,
    # discriminator="original",
    # generator="original",
    # generator_depth=4,
    lambda_pixel=100,
    # log_level="debug",
    # log_mode="w",
    lr=1,
    mass_max=5e12,
    mass_min=1e12,
    # may_resume=False,
    metrics=("mse", "l1"),
    n_cpu=2,
    n_epochs=600,
    n_gas_min=2000,
    num_filters=4,
    nvoxel=16,
    output_path=r"runs",
    patch_side=8,
    sample_interval=10,
    sim_name="TNG100-3",
    snap_num=99,
)
